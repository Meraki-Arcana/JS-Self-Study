// Exercises: Variables and Primitive Types Warmup - Javascript In Depth.

/*
    Create a constant variable of each of the following primitive types:

    —— String
    —— Number
    —— Boolean
    —— Null
    —— Undefined

    Use console.log to log out each vairable to the terminal at the end.

    */

    const name = "Marisa";
    const age = 52;
    const employed = true;
    const emptyThing = null;
    const anotherEmptyThing = undefined;

    console.log(name);
    console.log(age);
    console.log(employed);
    console.log(emptyThing);
    console.log(anotherEmptyThing);