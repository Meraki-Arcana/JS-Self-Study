/*

    What would each of these lines of code do:

    1. I think this will return TRUE.
    2. I think this will return FALSE.
    3. This will return hello friend.
    4. This will result in bye friend.

    This is a very challenging problem set. Once you see the results, try to see i you can see WHY it works that way. This will come in very handy in the future.

*/

true && console.log("moo");
false && console.log("moo moo?");
true || console.log("hello friend");
false || console.log("bye friend");

// The code that passed the test outputs information into the console, the ones that failed doesn't execute anything.
